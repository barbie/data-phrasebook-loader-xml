<?xml version="1.0"?>
 <!DOCTYPE phrasebook [
	       <!ELEMENT phrasebook (dictionary)*>              
	       <!ELEMENT dictionary (phrase)*>
               <!ATTLIST dictionary name CDATA #REQUIRED>
               <!ELEMENT phrase (#PCDATA)>
               <!ATTLIST phrase name CDATA #REQUIRED>
 ]>
 
 <phrasebook>
 <dictionary name="BASE">
   <phrase name="find_series">
     select title,author from books where class = :series
   </phrase>
   <phrase name="find_author">
     select title,author from books where author = :author
   </phrase>
   <phrase name="count_author">
     select count(1)
     from books
     where author = :author
   </phrase>
 </dictionary>
 </phrasebook>
